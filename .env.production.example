# TrackBuddy — Production Environment
# =====================================
# Copy this file to .env.production and fill in real values before deploying.
# NEVER commit real credentials to version control.

# ── Database (PostgreSQL) ──────────────────────────────────────────────────────
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://trackbuddy_prod:CHANGE_ME_STRONG_PASS@localhost:5432/trackbuddy_prod?schema=public"

# ── Redis ─────────────────────────────────────────────────────────────────────
# For cPanel: Use Upstash Redis (free tier) — https://upstash.com
# Format: redis://default:TOKEN@HOST:PORT
REDIS_URL="redis://localhost:6379"

# For Upstash (production recommended):
# UPSTASH_REDIS_REST_URL="https://YOUR-UPSTASH-ENDPOINT.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="YOUR_TOKEN"

# ── Authentication ────────────────────────────────────────────────────────────
# Must be at least 32 characters. Generate with: openssl rand -hex 32
JWT_SECRET="REPLACE_WITH_64_CHAR_RANDOM_STRING_USE_OPENSSL_RAND_HEX_32"
NEXTAUTH_SECRET="REPLACE_WITH_ANOTHER_STRONG_SECRET"
NEXTAUTH_URL="https://YOUR_DOMAIN.COM"

# ── Communication Services ────────────────────────────────────────────────────
# Resend (Email) — https://resend.com
RESEND_API_KEY="re_XXXXXXXXXXXX"
EMAIL_FROM="noreply@yourdomain.com"

# WhatsApp Business API (optional)
WHATSAPP_API_URL=""
WHATSAPP_API_TOKEN=""

# ── GPS / Wialon ──────────────────────────────────────────────────────────────
WIALON_API_URL="https://hst-api.wialon.com/wialon/ajax.html"
WIALON_TOKEN="YOUR_WIALON_TOKEN"

# ── App Config ────────────────────────────────────────────────────────────────
NODE_ENV="production"
PORT="3000"
